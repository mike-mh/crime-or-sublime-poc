//-
  This is the entry point for the entire application. This should be the .pug
  file that is not with another component in the app directories (aside from
  email templates).
doctype html
html(lang="en")
  head
    title
      | Crime or Sublime
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    meta(content="1111086175633223" property="fb:app_id")
    meta(content="www.something.com" property="og:url")
    meta(content="CRIME OR SUBLIME!" property="og:title")
    meta(content="http://crime-or-sublime.herokuapp.com/imgs/best-head.png" property="og:image")
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css")
    base(href="/")
    //- Include necessary libraries
    script(src="/shim.min.js")
    script(src="/zone.min.js")
    script(src="/Reflect.js")
    //- Use this to deal with [hidden] bug for now.
    style
      | [hidden] { display: none !important;}
    cos(id="cos-index-main")
      | Loading...
  //- Import the condensed CoS Angular app.
  script(src="/build.js")